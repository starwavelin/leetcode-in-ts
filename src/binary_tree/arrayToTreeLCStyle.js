/**
 * Similar to the arrayToTree.js, but I want to implement it in the LeetCode style.
 * For example, if I have an array:
 *  [1, null, 2, 3]
 * I should have a tree as:
 *               1
 *             /   \
 *          null    2
 *                 /
 *                3 
 */

import { TreeNode } from './arrayToTreeMyStyle.js';

const arrayToTree = (arr) => {
    
}

export {
    arrayToTree
}

// Test
const arr1 = [1, null, 2];
// console.log(arrayToTree(arr1));

const arr2 = [1, null, 2, 3];
// console.log(arrayToTree(arr2));

// const arr3 = [1, 2, 3, null, null, 4, 5];
// console.log(arrayToTree(arr3));